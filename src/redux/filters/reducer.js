const initialState = {
  searchName: '',
};

const filtersReducer = (state = initialState, actions) => {
  switch (actions.type) {
    default:
      return state;
  }
};

export default filtersReducer;
