const initialState = {
  contact: '',
};

const contactReducer = (state = initialState, actions) => {
  switch (actions.type) {
    default:
      return state;
  }
};

export default contactReducer;
